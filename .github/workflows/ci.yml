name: CI

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test & lint (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.os }}

      - name: Format
        if: runner.os == 'Linux' # formatting is platform-independent
        run: cargo fmt --all --check

      - name: Clippy (all features, warnings denied)
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Test (all features)
        run: cargo test --all-features

  dependency-free:
    name: Dependency-free library + CLI build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@v2

      # The library must not pull any third-party crate by default; only the
      # optional `cli` feature adds `oxideav-png`.
      - name: Build library (no features)
        run: cargo build

      - name: Assert the default build is dependency-free
        run: |
          extra=$(cargo tree -e normal --prefix none | grep -v '^anyd ' || true)
          if [ -n "$extra" ]; then
            echo "Unexpected default dependencies:"; echo "$extra"; exit 1
          fi

      - name: Build the anyd CLI (cli feature)
        run: cargo build --features cli --bin anyd

  features:
    name: Feature slices (warnings denied)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@v2

      # Every symbology alone, with encode-only / decode-only / both / scan.
      - name: Check feature slices
        run: ci/check-features.sh

      - name: Test a single-symbology build
        run: cargo test --no-default-features --features qr,encode,decode

      # Runs the heap-free encoders with the library built without `alloc`.
      - name: Test heap-free encoders (no alloc)
        run: cargo test --no-default-features --features encode,all-codes

  no-std:
    name: no_std builds (bare metal)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: thumbv7em-none-eabihf, thumbv6m-none-eabi

      - name: Cache
        uses: Swatinem/rust-cache@v2

      - name: No alloc
        run: cargo build --lib --target thumbv7em-none-eabihf --no-default-features --features encode,all-codes

      - name: alloc, every codec
        run: cargo build --lib --target thumbv7em-none-eabihf --no-default-features --features alloc,encode,decode,all-codes

      - name: alloc, no atomics
        run: cargo build --lib --target thumbv6m-none-eabi --no-default-features --features alloc,encode,decode,all-codes

  docs:
    name: Docs build (warnings denied)
    runs-on: ubuntu-latest
    env:
      RUSTDOCFLAGS: -D warnings -D rustdoc::broken-intra-doc-links
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@v2

      - name: cargo doc --no-deps --all-features
        run: cargo doc --no-deps --all-features
